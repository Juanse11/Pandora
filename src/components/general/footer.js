import React from "react";
import {
  Container,
  Grid,
  Segment
} from "semantic-ui-react";

const Footer = () => (
  <Segment inverted vertical style={{ padding: "5em 0em", background: 'transparent' }}>
    <Container>
      <Grid divided inverted stackable>
        <Grid.Row>
        
        </Grid.Row>
      </Grid>
    </Container>
  </Segment>
);

export default Footer
